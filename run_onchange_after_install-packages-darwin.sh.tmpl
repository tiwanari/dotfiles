{{- if eq .chezmoi.os "darwin" -}}
#!/bin/bash
# Brewfile hash: {{ include "dot_config/homebrew/Brewfile" | sha256sum }}
set -eu

brew bundle install --no-upgrade --file={{ .chezmoi.homeDir }}/.config/homebrew/Brewfile
{{ end -}}
